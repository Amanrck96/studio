# SMTP configuration for contact form emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username@example.com
SMTP_PASS=your-smtp-password

# Destination address for contact form (defaults to SMTP_USER if not set)
CONTACT_TO_EMAIL=recipient@example.com

# Next.js configuration
NEXT_PUBLIC_SITE_URL=https://localhost:9002
